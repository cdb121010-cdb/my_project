# 커패시터 기반 시냅스 셀(3T1C) 설계 전략 — 논문 해설

> 대상 논문: Byoungwoo Lee *et al.*, **“Design Strategies of Capacitor-Based Synaptic Cell for High-Efficiency Analog Neural Network Training”**, *Advanced Intelligent Systems*, 2025, 7, 2400600. DOI: [10.1002/aisy.202400600](https://doi.org/10.1002/aisy.202400600)
>
> 참고: 파일명에는 2024가 있으나, 저널 표기는 2025년이다. 2024년은 논문 저작권/DOI 식별자에 해당한다.

## 한 문장 요약

아날로그 AI 학습용 **3T1C(트랜지스터 3개 + 커패시터 1개)** 시냅스 셀은 매우 이상적인 가중치 업데이트를 제공하지만, 작게 만들면 전하 누설(retention)과 상태 수가 부족해진다. 따라서 실용적인 고집적 구현에는 **고밀도·저누설 커패시터**와 **낮고 서로 맞춰진 on-current, 극저 off-current를 갖는 보완형 트랜지스터**가 필요하다는 논문이다.

## 1. 왜 이 연구가 필요한가

딥러닝 학습에서는 반복적으로 다음 두 계산을 수행한다.

1. **행렬–벡터 곱(MVM)**: 추론·순전파·역전파 계산
2. **외적(outer product) 가중치 업데이트**: 학습 중 모든 가중치를 조정

GPU/CPU 방식은 가중치를 메모리와 연산기 사이에서 계속 옮겨야 하므로 에너지와 시간이 많이 든다. 아날로그 인메모리 컴퓨팅(AIMC)은 가중치를 크로스바 어레이 셀에 저장하고, 전압과 전류의 물리 법칙으로 MVM을 병렬 수행해 이 병목을 줄인다.

기존 RRAM, PCM, MRAM 같은 비휘발성 메모리(NVM)는 고집적에는 유리하지만, 학습 관점에서는 다음 문제가 있다.

- 증가/감소 업데이트가 비대칭적임
- 소자별·사이클별 변동성이 큼
- 비선형성, 제한된 endurance/retention이 있음

이 논문의 관점에서 좋은 학습용 RPU(resistive processing unit)는 단순히 “메모리”가 아니라, 가중치를 아주 작고 **선형적·대칭적으로·오래** 조절할 수 있는 소자여야 한다.

## 2. 핵심 아이디어: 저항 대신 커패시터 전압으로 가중치를 저장한다

3T1C 셀은 가중치를 저항값이 아니라 커패시터 전압 `Vcap`에 저장한다.

```text
                    충전 PFET
행/열 업데이트 펄스 ───┤ ├──┐
                              │
                           [ C ]  Vcap = 가중치 상태
                              │
행/열 업데이트 펄스 ───┤ ├──┘
                   방전 NFET

Vcap ──> 읽기 NFET의 gate ──> Iread
                              │
                         열선에서 전류 합산
```

- **PFET**: 선택된 셀의 커패시터를 충전 → `Vcap` 증가 → 가중치 증가
- **NFET**: 선택된 셀의 커패시터를 방전 → `Vcap` 감소 → 가중치 감소
- **읽기 NFET**: `Vcap`을 gate 전압으로 받아 읽기 전류 `Iread`로 변환
- **커패시터**: 실제 가중치 상태를 보존하는 저장소

읽기 경로와 쓰기 경로가 분리되어 있으므로 읽기는 비파괴적이다. 또한 행과 열의 확률적 펄스가 동시에 들어온 교차점만 갱신하는 **half-select 방식**으로, 어레이 전체에서 외적 업데이트를 병렬 실행할 수 있다.

## 3. 논문이 정의한 세 가지 핵심 성능 지표

| 지표 | 의미 | 나빠지면 생기는 일 | FP32급 MNIST 학습을 위한 논문 내 기준 |
|---|---|---|---|
| Retention `τ` | 저장된 `Vcap`이 누설 없이 유지되는 정도 | 가중치가 0 또는 최솟값/최댓값으로 저절로 쏠림 | 0으로 쏠릴 때 `τ ≥ 5.6×10⁶` update cycles (약 1.1 s), 양 끝으로 쏠릴 때 `τ ≥ 5.6×10⁷` cycles (약 11 s) |
| Update asymmetry `α` | 증가 펄스와 감소 펄스의 크기가 얼마나 대칭적인가 | 학습이 한쪽 방향으로 편향됨 | `α < 0.56` |
| Number of states `ν` | 구분 가능한 가중치 단계 수 | 업데이트 해상도가 거칠어짐 | `ν ≥ 1778` |

여기서 시간 환산은 논문의 1 update cycle = 200 ns 가정에 따른다. 이 기준들은 50,000개 MNIST 이미지, 3층 fully connected network, 50 epoch 조건의 시스템 시뮬레이션 결과다. 즉 모든 모델·워크로드에 보편적인 절대 기준이라기보다, 이 논문이 제시하는 유용한 설계 목표다.

### 3.1 Retention: 가장 어려운 문제

쓰기 PFET와 NFET는 꺼져 있어도 아주 작은 off-current를 흘린다. 두 누설 전류가 다르면 커패시터가 한쪽으로 충전 또는 방전되어 가중치가 학습 명령 없이 변한다.

- NFET 누설이 더 크면 `Vcap`이 감소하는 방향으로 drift
- PFET 누설이 더 크면 `Vcap`이 증가하는 방향으로 drift
- 둘을 완전히 맞추거나, 둘 다 극도로 작게 만들어야 함

대략적으로 `τ`는 커패시턴스 `C`에 비례하고 누설전류 `Ileak`에 반비례한다. 그래서 큰 커패시터와 작은 off-current가 유리하다.

### 3.2 대칭성: 단순히 PFET/NFET 크기를 맞추면 충분하지 않다

충전과 방전 전류가 같은 `Vcap` 지점을 **symmetry point (`VSP`)**라고 한다. 논문은 이 지점을 신경망 가중치의 0에 매핑해야 한다고 설명한다. `Vcap`이 이 점에서 멀어질수록 PFET와 NFET 전류 차이 때문에 증가/감소 업데이트가 더 비대칭이 된다.

문제는 retention과 대칭성이 서로 충돌할 수 있다는 점이다. PFET와 NFET의 off-current를 같게 하려고 크기를 바꾸면, 이동도 차이 때문에 on-current가 달라져 업데이트 비대칭이 커질 수 있다.

### 3.3 상태 수: 커패시터가 커질수록 유리하다

한 펄스가 옮기는 전하가 작을수록 더 많은 세밀한 단계를 만들 수 있다. 큰 `C`는 같은 전하 변화가 만드는 전압 변화 `ΔVcap`을 작게 하므로 상태 수 `ν`를 높인다. 동시에 누설로 인한 전압 변화를 늦춰 retention도 개선한다.

**따라서 C를 키우면 `ν`와 `τ`가 함께 좋아진다.** 반대로 셀 면적을 줄이면 이 두 값이 함께 나빠진다.

## 4. 설계 전략

주어진 셀 면적 안에서 논문이 제안하는 기본 전략은 다음과 같다.

1. 가능한 한 많은 면적을 커패시터에 배정한다.
2. 트랜지스터 폭 `W`는 가능한 작게 하여 programming on-current를 낮춘다.
3. NFET 길이 `LN`은 가능한 길게 한다.
4. PFET 길이 `LP`는 `VSP`에서 충전·방전 on-current가 일치하도록 조정한다.

전류를 낮추면 한 펄스의 `ΔVcap`이 작아져 상태 수가 늘어난다. 하지만 전류를 무한정 낮출 수는 없고, 실제 공정에서 누설과 구동 전류를 동시에 만족해야 한다.

## 5. Si CMOS로 설계했을 때의 결과와 한계

논문은 SKY130 130 nm CMOS PDK, 1.8 V low-`Vt` NMOS/PMOS, SiO₂ MIM 커패시터(밀도 약 `2 fF/µm²`)를 사용해 셀을 모델링했다.

`2 × 2 µm²` 커패시터(8 fF) 조건에서 최적화한 결과는 다음과 같다.

- `α+ = 0.20`, `α− = 0.12`: 비대칭성은 기준 범위에 비교적 들어옴
- `ν+ = ν− = 6`: 필요한 1778 states에 크게 못 미침
- `τ = 7.2×10⁴ cycles`: 필요한 retention에 크게 못 미침

즉 **회로와 트랜지스터 크기만 잘 조정해서는 작은 SiO₂ MIM 기반 셀을 학습용 RPU로 만들 수 없다.**

셀/커패시터의 한 변 길이를 2, 4, 8, 16 µm로 키워 MNIST 학습을 비교했을 때도, 논문은 약 `16 × 16 µm²` 커패시터가 되어야 충분한 상태 수와 retention을 확보한다고 관찰했다. 고집적 크로스바에 원하는 면적과는 거리가 있다.

## 6. 재료/소자 차원의 해결 방향

논문의 중요한 메시지는 retention 문제를 회로 기법만으로 해결할 수 없다는 것이다. 커패시터와 트랜지스터 자체를 개선해야 한다.

### 트랜지스터 후보

| 조합/후보 | 장점 | 한계 또는 의미 |
|---|---|---|
| ZTO n-FET + pentacene p-FET | 매우 낮은 off-current. 측정된 pentacene off-current는 1.71 pA, ZTO는 0.014 pA 이하 | on-current 차이가 커서 양방향 대칭성을 맞추기 어려움 |
| IGZO n-FET + 주석 할라이드 페로브스카이트 p-FET | 이동도가 각각 0.62, 0.36 cm²/V·s로 비슷하여 충·방전 전류 매칭에 유리 | 이 조합의 장기 신뢰성 및 공정 통합은 후속 과제 |
| IGZO 계열 | 극히 낮은 off-current가 보고되어 retention에 특히 유망 | 동등하게 저누설인 p-FET가 아직 큰 과제 |

### 커패시터 후보

일반 SiO₂ MIM보다 고유전율(high-k), 다중 금속판 적층, 3D 구조, deep-trench 커패시터가 유리하다. 논문은 예시로 deep-trench capacitor의 `235 fF/µm²`와 `0.01 fA/µm²` 이하의 누설 전류 밀도를 든다.

## 7. 고집적 “이상적 3T1C”를 위한 정량적 목표

배선까지 포함해 약 `1 × 1 µm²` 셀을 목표로 한다면, SiO₂ MIM의 2 fF 수준 커패시터로는 요구되는 소자 전류가 비현실적이다.

- 필요 on-current: **0.1 nA 이하**
- 필요 off-current: **10 aA 이하**

반면 235 fF 수준의 고밀도 deep-trench 커패시터를 쓸 수 있다면 목표가 완화된다.

- 필요 on-current: **0.13 µA 이하**
- 필요 off-current: **5.8 fA 이하**

이 차이가 논문의 결론을 가장 잘 보여준다. 고집적 구현의 핵심은 “FET를 잘 설계하는 것”만이 아니라, **충분히 큰 C를 아주 작은 면적에서 제공하는 커패시터 공정**이다.

## 8. 결과를 어떻게 해석해야 하는가

이 논문은 완성된 대규모 상용 칩을 발표한 논문이라기보다, 다음을 수행한 **설계공간 탐색 및 기술 로드맵 논문**에 가깝다.

- 셀 수준 모델링: FET 특성, 커패시터, 누설을 3T1C 동작에 반영
- 시스템 수준 검증: 모델링한 `α`, `ν`, `τ`를 사용해 MNIST 학습 정확도를 시뮬레이션
- 재료 실험: ZTO, pentacene, IGZO, perovskite FET의 전달 특성을 측정

따라서 “IGZO/페로브스카이트 조합이 이미 완벽한 3T1C 어레이로 실증되었다”로 읽으면 안 된다. 이들은 유망한 소자 후보이며, 완성형 집적 소자/대형 어레이 검증은 남아 있다.

## 9. 읽을 때 잡아야 할 논리 흐름

```text
아날로그 학습에는 이상적인 가중치 업데이트가 필요
                ↓
커패시터 기반 3T1C는 선형성·대칭성에서 NVM보다 유리
                ↓
그러나 커패시터는 누설되고, 작은 셀은 C가 작다
                ↓
학습 정확도는 α(대칭성), ν(상태 수), τ(retention)에 의해 제한
                ↓
작은 Si CMOS 셀은 ν와 τ를 만족하지 못함
                ↓
고밀도·저누설 C + 저 on/off-current 보완형 FET가 필요
```

## 10. 용어 빠른 정리

- **AIMC (Analog In-Memory Computing)**: 메모리 위치에서 아날로그 물리량으로 연산하는 방식
- **RPU (Resistive Processing Unit)**: 크로스바의 한 가중치 셀을 부르는 학습용 소자 단위
- **3T1C**: 트랜지스터 3개, 커패시터 1개로 된 셀
- **Crossbar array**: 행·열 교차점마다 가중치 셀을 놓아 병렬 MVM/업데이트를 하는 구조
- **On-current / off-current**: 트랜지스터가 켜졌을 때 / 꺼졌을 때의 전류. 여기서는 둘 다 낮추되, PFET/NFET의 on-current는 서로 맞춰야 한다.
- **Retention**: 저장한 전압/가중치가 누설 없이 유지되는 시간
- **Half-select**: 행 또는 열만 선택된 이웃 셀은 갱신하지 않고, 둘 다 선택된 교차점만 갱신하는 기법
- **BEOL-compatible**: CMOS 트랜지스터를 만든 뒤의 금속 배선 공정 층에서도 통합 가능한 공정 특성

## 11. 스스로 점검할 질문

1. 이 셀에서 가중치는 저항값이 아니라 무엇으로 저장되는가?
2. 읽기 경로를 업데이트 경로와 분리하면 어떤 이점이 있는가?
3. 커패시턴스 `C`가 커지면 왜 상태 수와 retention이 함께 좋아지는가?
4. PFET/NFET의 off-current를 맞추는 일이 왜 update symmetry와 충돌할 수 있는가?
5. 작은 셀에 고밀도 커패시터가 필요한 이유는 무엇인가?
6. 이 논문은 완성 칩의 실증보다 어떤 설계 목표를 제시하는가?

## 결론

3T1C는 가중치 업데이트의 대칭성과 선형성 면에서 학습용 아날로그 메모리의 매력적인 구조다. 하지만 셀 면적을 줄이는 순간 커패시터가 작아지고 누설 영향이 커져, 상태 수와 retention이 동시에 부족해진다. 논문은 이 병목을 정량화하고, 향후 핵심 과제를 **고용량·저누설 커패시터**, **낮은 off-current**, **매칭된 낮은 on-current의 n/p 트랜지스터**로 명확히 제시한다.











현재 코드베이스는 JSON 파일에 메모를 저장하는 작은 Python CLI 패키지입니다.

## 구성

- 진입점: [cli.py](/home/choidabin/gpt/src/notes_cli/cli.py:56)
- 패키징/명령 등록: [pyproject.toml](/home/choidabin/gpt/pyproject.toml:1)
- 사용법: [README.md](/home/choidabin/gpt/README.md:1)
- 테스트: [test_cli.py](/home/choidabin/gpt/tests/test_cli.py:1)

지원 명령은 다음 네 가지입니다.

- `add "내용"`: 메모 생성
- `list`: 전체 조회
- `show ID`: 메모 하나 조회
- `remove ID`: 메모 삭제

기본 저장소는 실행 폴더의 `.notes.json`이며, `--file`로 변경할 수 있습니다.

## 구현 평가

구조는 간결하고 목적에 맞습니다.

- 표준 라이브러리만 사용하므로 가볍습니다.
- `argparse` 기반 CLI와 `Path` 기반 파일 처리가 명확합니다.
- 없는 저장 파일은 빈 목록으로 처리합니다. ([cli.py](/home/choidabin/gpt/src/notes_cli/cli.py:11))
- 없는 메모 조회/삭제는 종료 코드 `1`로 처리합니다. ([cli.py](/home/choidabin/gpt/src/notes_cli/cli.py:72))
- `pyproject.toml`에 `notes` 실행 명령이 등록돼 있어 설치 후 어느 폴더에서나 실행 가능합니다. ([pyproject.toml](/home/choidabin/gpt/pyproject.toml:11))

직접 실행해 본 메모 생성 → 조회 → 삭제 → 없는 메모 조회 흐름은 정상 동작했습니다. 소스 컴파일도 통과했습니다.

## 보완하면 좋은 점

1. 잘못된 JSON 또는 손상된 데이터 처리
   현재 JSON이 깨졌거나, 리스트 안의 항목에 `id`/`text`가 없으면 traceback이 그대로 노출됩니다. 사용자 친화적인 오류 메시지와 종료 코드를 추가하는 편이 좋습니다.

2. 원자적 저장 부재
   [save_notes](/home/choidabin/gpt/src/notes_cli/cli.py:22)는 대상 파일에 바로 씁니다. 저장 중 프로세스가 중단되면 JSON 파일이 손상될 수 있습니다. 임시 파일에 쓴 뒤 교체하는 방식이 안전합니다.

3. 동시 실행 충돌
   두 `add` 프로세스가 동시에 실행되면 둘 다 같은 다음 ID를 계산하거나 한쪽 저장 결과가 다른 쪽을 덮어쓸 수 있습니다. 개인용 단일 프로세스 도구에는 괜찮지만, 확장 시 잠금이 필요합니다.

4. 테스트 범위
   현재 lifecycle 하나만 검증합니다. ([test_cli.py](/home/choidabin/gpt/tests/test_cli.py:19))
   빈 목록, 여러 메모의 ID 증가, 잘못된 JSON, 잘못된 ID/명령, `--file`의 상위 디렉터리 생성 등을 추가하면 좋습니다.

5. 테스트 의존성 선언
   테스트는 `pytest` 형식이지만 개발 의존성에 명시돼 있지 않아 이 환경에서는 `python3 -m pytest`가 실행되지 않았습니다 (`No module named pytest`). `pyproject.toml`에 optional test dependency를 추가하면 재현성이 좋아집니다.

전반적으로는 교육용 또는 개인용 메모 CLI로 깔끔한 최소 구현입니다. 운영 수준으로 발전시키려면 파일 오류 처리와 원자적 저장부터 보완하는 것이 우선입니다.






























현재 코드베이스는 JSON 파일에 메모를 저장하는 작은 Python CLI 패키지입니다.

## 구성

- 진입점: [cli.py](/home/choidabin/gpt/src/notes_cli/cli.py:56)
- 패키징/명령 등록: [pyproject.toml](/home/choidabin/gpt/pyproject.toml:1)
- 사용법: [README.md](/home/choidabin/gpt/README.md:1)
- 테스트: [test_cli.py](/home/choidabin/gpt/tests/test_cli.py:1)

지원 명령은 다음 네 가지입니다.

- `add "내용"`: 메모 생성
- `list`: 전체 조회
- `show ID`: 메모 하나 조회
- `remove ID`: 메모 삭제

기본 저장소는 실행 폴더의 `.notes.json`이며, `--file`로 변경할 수 있습니다.

## 구현 평가

구조는 간결하고 목적에 맞습니다.

- 표준 라이브러리만 사용하므로 가볍습니다.
- `argparse` 기반 CLI와 `Path` 기반 파일 처리가 명확합니다.
- 없는 저장 파일은 빈 목록으로 처리합니다. ([cli.py](/home/choidabin/gpt/src/notes_cli/cli.py:11))
- 없는 메모 조회/삭제는 종료 코드 `1`로 처리합니다. ([cli.py](/home/choidabin/gpt/src/notes_cli/cli.py:72))
- `pyproject.toml`에 `notes` 실행 명령이 등록돼 있어 설치 후 어느 폴더에서나 실행 가능합니다. ([pyproject.toml](/home/choidabin/gpt/pyproject.toml:11))

직접 실행해 본 메모 생성 → 조회 → 삭제 → 없는 메모 조회 흐름은 정상 동작했습니다. 소스 컴파일도 통과했습니다.

## 보완하면 좋은 점

1. 잘못된 JSON 또는 손상된 데이터 처리
   현재 JSON이 깨졌거나, 리스트 안의 항목에 `id`/`text`가 없으면 traceback이 그대로 노출됩니다. 사용자 친화적인 오류 메시지와 종료 코드를 추가하는 편이 좋습니다.

2. 원자적 저장 부재
   [save_notes](/home/choidabin/gpt/src/notes_cli/cli.py:22)는 대상 파일에 바로 씁니다. 저장 중 프로세스가 중단되면 JSON 파일이 손상될 수 있습니다. 임시 파일에 쓴 뒤 교체하는 방식이 안전합니다.

3. 동시 실행 충돌
   두 `add` 프로세스가 동시에 실행되면 둘 다 같은 다음 ID를 계산하거나 한쪽 저장 결과가 다른 쪽을 덮어쓸 수 있습니다. 개인용 단일 프로세스 도구에는 괜찮지만, 확장 시 잠금이 필요합니다.

4. 테스트 범위
   현재 lifecycle 하나만 검증합니다. ([test_cli.py](/home/choidabin/gpt/tests/test_cli.py:19))
   빈 목록, 여러 메모의 ID 증가, 잘못된 JSON, 잘못된 ID/명령, `--file`의 상위 디렉터리 생성 등을 추가하면 좋습니다.

5. 테스트 의존성 선언
   테스트는 `pytest` 형식이지만 개발 의존성에 명시돼 있지 않아 이 환경에서는 `python3 -m pytest`가 실행되지 않았습니다 (`No module named pytest`). `pyproject.toml`에 optional test dependency를 추가하면 재현성이 좋아집니다.

전반적으로는 교육용 또는 개인용 메모 CLI로 깔끔한 최소 구현입니다. 운영 수준으로 발전시키려면 파일 오류 처리와 원자적 저장부터 보완하는 것이 우선입니다.
